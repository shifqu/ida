"""Relations app configuration."""

from django.apps import AppConfig


class RelationsConfig(AppConfig):
    """Represent the relations AppConfig."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.relations"
